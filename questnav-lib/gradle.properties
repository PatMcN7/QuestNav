# QuestNavLib Build Configuration

# Vendor dependency configuration
vendorUuid = 'a706fe68-86e5-4aed-92c5-ce05aca007f0'
mavenGroupId = 'gg.questnav'
javaArtifactId = 'questnavlib-java'
cppArtifactId = 'questnavlib-cpp'

# Default Version Configuration (should be overridden via -P flags)
questnavVersion=2025-1.0.0
releaseType=dev
frcYear=2025
wpilibVersion=2025.3.2

# Build Configuration
org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=512m
org.gradle.parallel=true
org.gradle.caching=true

# Publishing Configuration (set via command line or CI)
mavenRepoUrl=https://maven.questnav.gg
# mavenUsername and mavenPassword are set by flags in CI

# Java Configuration
java.incremental=true